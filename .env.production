# Production env for the FRONTEND DEMO deployment.
# Forces in-memory demo mode so the app runs fully without a database.
# No secrets here — safe to commit.
#
# To switch to a real database later:
#   1. Remove DEMO_MODE
#   2. Set DATABASE_URL (Vercel dashboard env var recommended)
DEMO_MODE=1
